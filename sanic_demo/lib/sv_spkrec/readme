编译命令：
g++ -O3 -Wall -shared -std=c++11 -fPIC $(python -m pybind11 --includes) feature_db.cpp -o feature$(python-config --extension-suffix)


使用示例：

-加载
from lib.db.cpp_lib.feature import FeatureDB

-初始化类对象
/**
* @FeatureDB
* @brief 构造函数，定义存放index和特征的文件路径
* @param 参数1  存放特征的文件路径
* @param 参数2  存放索引的文件路径
* @param 参数3  阈值
*/
feature_obj = FeatureDB("dish.db", "dish.index"， 0.289)

-搜索特征数据库
/**
* @brief 查询特征数据库
* @param 参数1 ndarray特征
*
* @return 字符串，格式为‘特征索引|相似度’  example: "3269|0.95847"
*/
feature_obj.search(feature)

-插入特征到数据库
/**
* @brief 插入特征数据库
* @param 参数1  ndarray特征
* @param 参数2  索引名
*
* @return (std::string）返回结果字符串  example: "success"
*/
feature_obj.add(feature, id)